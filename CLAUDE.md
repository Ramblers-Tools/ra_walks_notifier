# RA Walks Notifier (client) — Instructions for Claude Code

## Vault linkage
This repo's knowledge base lives in the vault, not in this repo:
- Vault notes: `~/vault/ra-walks-notifier/`
- Architecture decisions: `~/vault/ra-walks-notifier/architecture/decisions.md`
- Session logs: `~/vault/ra-walks-notifier/logs/`
- Graphify knowledge graph: `~/vault/ra-walks-notifier/graph/` (`GRAPH_REPORT.md`, `graph.json`, per-entity notes)

See `~/vault/CLAUDE.md` for the full zettelkasten rules and the `/save` / `/resume` session commands — they apply here using the vault paths above.

## Never do
- Don't create notes or logs inside this repo — they belong in the vault path above
- Don't move this repo into the vault
